name: aduanext_ui
description: "Shared AduaNext UI package — Atomic Design widgets y tema (presentación pura, sin DTOs ni Riverpod)."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.0.2
  aduanext_domain:
    path: ../../libs/domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
