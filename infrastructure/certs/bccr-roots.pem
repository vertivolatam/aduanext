# Placeholder — see ./README.md for provenance.
#
# VRTV-58b will replace this file with the real BCCR trust bundle
# downloaded from https://www.firmadigital.go.cr/repositorio/certificados.html
# (and pin its SHA-256 fingerprint in bccr-roots.pem.sha256).
#
# Until then, the LocalXmlSignatureVerifier runs in `degraded` mode
# (structural check only) and every audit payload carries
# `degraded: true` so the UI can warn the operator.
